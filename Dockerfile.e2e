FROM rust:1-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends tmux ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
COPY . .

RUN cargo build -p pi-cli

CMD ["./scripts/e2e-tmux.sh"]
